API_DOMAIN=api.example.com
ACM_DATA_DIR=./.local/production-data
ACM_DOCKER_PLATFORM=linux/amd64
JWT_SECRET=
DISCORD_SECRET=
PARALLEL_JOB_COUNT=1
FRONTEND_ORIGIN=https://acm.example.com
DISCORD_CLIENT_ID=replace-with-discord-client-id
# envsitter: Must use FRONTEND_ORIGIN's normalized scheme, host, and effective port, with /auth/discord and no credentials, query, or fragment; use HTTPS in production.
DISCORD_REDIRECT_URI=https://acm.example.com/auth/discord
